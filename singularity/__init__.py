"""
SINGULARITY — Autonomous Enterprise Runtime
============================================

Autonomous Enterprise Runtime.
Built from ground up. Not a patch. Not a clone. An organism.

Subsystems:
    bus     — Event bus (the nervous system)
    config  — SPINE configuration
    cortex  — Brain (agent loop, planner, router)
    nerve   — Communications (channel adapters)
    memory  — Memory (COMB native)
    immune  — Health + recovery (POA pattern)
    sinew   — Tools (execution engine)
    voice   — LLM providers
    csuite  — C-Suite (native agent spawning)
    pulse   — Scheduler (cron, triggers, timers)
"""

__version__ = "0.1.0"
__codename__ = "singularity"
